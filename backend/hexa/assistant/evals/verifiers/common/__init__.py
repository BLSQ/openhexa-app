"""Helpers shared by the Python verifiers: AST walking and symbol resolution.

Both are specific to Python source. Verifiers for another artifact, such as
GraphQL documents or SQL, would need their own helpers rather than these.
"""
