"""The create-pipeline suite: CreatePipelineAgent on template-derived cases.

Every suite package holds the same three pieces. `schemas.py` is the contract
with the hosted dataset, `task.py` runs one case against the agent, and
`evaluators.py` scores what comes back.
"""
