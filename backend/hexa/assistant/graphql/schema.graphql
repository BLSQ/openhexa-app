type AssistantMessagePage {
  items: [AssistantMessage!]!
  totalItems: Int!
  totalPages: Int!
}

type AssistantConversation {
  id: UUID!
  createdAt: DateTime!
  updatedAt: DateTime!
  totalInputTokens: Int!
  totalOutputTokens: Int!
  cost: Int! # in microdollars (millionths of a dollar)
  messages(page: Int = 1, perPage: Int = 20): AssistantMessagePage!
}

type AssistantMessage {
  id: UUID!
  createdAt: DateTime!
  role: String!
  content: String!
  inputTokens: Int
  outputTokens: Int
  toolInvocations: [AssistantToolInvocation!]!
}

type AssistantToolInvocation {
  id: UUID!
  createdAt: DateTime!
  toolName: String!
  toolInput: JSON!
  toolOutput: JSON
  success: Boolean!
}

extend type Workspace {
  assistantConversations: [AssistantConversation!]!
}

input CreateAssistantConversationInput {
  workspaceSlug: String!
  instructionSet: String
}

input SendAssistantMessageInput {
  conversationId: UUID!
  message: String!
}

type SendAssistantMessageResult {
  success: Boolean!
  errors: [String!]!
  conversation: AssistantConversation
  message: AssistantMessage
}

extend type Mutation {
  createAssistantConversation(input: CreateAssistantConversationInput!): AssistantConversation
  sendAssistantMessage(input: SendAssistantMessageInput!): SendAssistantMessageResult!
}

extend type Query {
  assistantConversation(id: UUID!): AssistantConversation
}

extend type Me {
  assistantMonthlyLimitExceeded: Boolean!
}
