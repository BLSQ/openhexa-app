enum ActivityStatus {
  SUCCESS
  PENDING
  RUNNING
  ERROR
  UNKNOWN
}

type Activity {
  description: String!
  occurredAt: DateTime!
  url: URL!
  status: ActivityStatus!
}


extend type Query {
  datasources: Int!
  totalNotebooks: Int!
  pipelines: Int!
  lastActivities: [Activity!]
}