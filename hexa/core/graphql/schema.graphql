enum ActivityStatus {
  SUCCESS
  PENDING
  RUNNING
  ERROR
  UNKNOWN
}

type Activity {
  description: String!
  occurredAt: DateTime!
  url: URL!
  status: ActivityStatus!
}


extend type Query {
  datasources: Int!
  notebooks: Int!
  pipelines: Int!
  lastActivities: [Activity!]
}