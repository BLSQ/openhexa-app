query PipelineConfigureRunPage($pipelineId: UUID!) {
  dag(id: $pipelineId) {
    id
    label
    externalId
    template {
      sampleConfig
      description
    }
    description
    ...PipelineRunForm_dag
  }
}
