query getPipelineParameterChoices(
    $pipelineVersionId: UUID!
    $parameterCode: String!
) {
    pipelineParameterChoices(
        pipelineVersionId: $pipelineVersionId
        parameterCode: $parameterCode
    )
}
