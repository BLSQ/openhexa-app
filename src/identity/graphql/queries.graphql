query GetUser {
  me {
    authorizedActions
    features {
      code
      config
    }
    user {
      email
      id
      firstName
      lastName
      displayName
      avatar {
        initials
        color
      }
    }
  }
}
