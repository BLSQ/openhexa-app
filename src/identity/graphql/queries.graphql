query GetUser {
  me {
    permissions {
      adminPanel
      superUser
    }
    features {
      code
      config
    }
    user {
      email
      id
      firstName
      lastName
      displayName
      avatar {
        initials
        color
      }
    }
  }
}
